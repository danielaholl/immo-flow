# Claude Code Projektnotizen

## Datenbank

- **Keine Supabase-Datenbank verwenden** - Wir nutzen ausschließlich die lokale PostgreSQL-Datenbank
- Alle Migrationen werden gegen die lokale PostgreSQL-Datenbank durchgeführt
- Migrationsdateien befinden sich in `packages/database/migrations/`
